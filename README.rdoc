To run the application type the following:

rake db:migrate
rails server

Then visit localhost:3000 in your browser.
You will be presented with a web page that will
let you upload a tab separated file.


To test the application type the following:

rake db:migrate (if you haven't already do so earlier)
rake test
